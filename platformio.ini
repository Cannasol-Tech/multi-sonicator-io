; Root PlatformIO Project Configuration (delegates to config/platformio.ini)

[platformio]
extra_configs =
  config/platformio.ini
include_dir = include
src_dir = src
lib_dir = lib
boards_dir = boards

; Keep default env to ensure pio sees a configuration at the repo root
; All real settings come from config/platformio.ini via extra_configs

[env]

[extra]

