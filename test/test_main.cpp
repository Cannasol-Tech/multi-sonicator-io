/**
 * @file test_main.cpp
 * @brief Main test runner for Multi-Sonicator I/O Controller
 * @author Cannasol Technologies
 * @date 2025-07-17
 * @version 1.0.0
 */

// Disabled custom Unity main. PlatformIO/Unity will generate per-suite mains.
// This avoids undefined references when tests are split across directories.
// If you need a custom aggregator test runner, create a dedicated test suite
// directory and add its own main there.
// Intentionally left blank.